# Monorepo Workspace

This repository now has a top-level workspace layout:

- `project-a/` – existing Chalmers app project (React Native + admin app)
- `project-b/` – empty folder for a second project

Run existing project commands from inside `project-a/`.
